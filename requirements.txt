# Local wheel files
./wheelhouse/torq_runtime-0.dev0+20260328t193647z.g0b9d557-cp312-cp312-linux_aarch64.whl
./wheelhouse/llama_cpp_python-0.3.16-cp312-cp312-linux_aarch64.whl

# Other standard dependencies from PyPI
numpy<2.0
Pillow
PyQt5>=5.15
psutil>=5.9
pyserial>=3.5

# Voice input (optional; enabled when CORAL_VOICE=stub|moonshine).
# Requires libportaudio2 system package for sounddevice.
#
# The Moonshine ASR backend (CORAL_VOICE=moonshine) reuses the shared
# runtime that ships under speech_to_text/ + utils/ at the repo root
# (added in sl2610-examples PR #8). The deps for that backend
# (onnxruntime, tokenizers, ml_dtypes, requests, tqdm, huggingface_hub)
# live in speech_to_text/requirements.txt — install that file in addition
# to this one when enabling Moonshine. The stub backend only needs the
# two dependencies below.
sounddevice>=0.4
silero-vad-notorch
